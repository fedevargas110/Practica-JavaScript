// Diferencia entre Metodos y Funciones

const n1 = 20;
const n2 = '20';

console.log( parseInt(n2)); // Esto es una funcion porque esta la funcion seguida de la varible en parentesis.
console.log(n2.toString()); // Esto es un metodo porque esta primero la variable seguido de la funcion.