// Booleans sireven para cuando esta autenticado el usario mostrar mas cosas o no

const boolean = true;
const boolean2 = false;

console.log(typeof boolean);
console.log(boolean2);

// typeof sirve para ver que tipo de dato estoy utilizando string, boolen, etc.

// Creando un costructor de booleans
const boolean3 = new Boolean(true);
console.log(boolean3);